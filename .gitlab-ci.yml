image: maven:3.8.8-openjdk-21-slim

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  paths:
    - .m2/repository

stages:
  - build
  - test
  - integration-test

build:
  stage: build
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn test